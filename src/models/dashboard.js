// import { myCity, queryWeather, query } from '../services/dashboard'

// import { parse } from 'qs'

export default {
  namespace: 'dashboard',
  state: {
   tester:1
  },
  subscriptions: {
   
  },
  effects: {
    
    // *queryMenu({
    //   payload,
    // },{call, put }){
    //   const data = yield call(menu, parse(payload))
    //   yield put({ type: 'queryMenu1', payload: { ...data } })
    // },
  
  },
  reducers: {
    
    // queryMenu1 (state, action) {
    //   return {
    //     ...state,
    //     ...action.payload,
    //   }
    // },
    
  },
}
